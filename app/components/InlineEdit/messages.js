/*
 * InlineEdit Messages
 *
 * This contains all the text for the InlineEdit component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.InlineEdit';

export default defineMessages({
  inputValue: {
    id: `${scope}.inputValue`,
    defaultMessage: `{inputValue}`,
  },
});
