/*
 * ReactFinalForm Messages
 *
 * This contains all the text for the ReactFinalForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ReactFinalForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ReactFinalForm container!',
  },
});
