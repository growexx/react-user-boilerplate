/*
 * TwoFactorAuthentication Messages
 *
 * This contains all the text for the TwoFactorAuthentication container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TwoFactorAuthentication';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TwoFactorAuthentication container!',
  },
});
